//@ts-ignore
import { classScroll } from 'index';

describe("(classScroll) add or remove class at certain scroll margins (on fixed items)", () => {
	test("MANUAL CHECK via 'npm run serve', use /index.html and /tests/index.ts", () => {
		expect(classScroll).toBeTruthy();
	});
});
